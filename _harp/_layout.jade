doctype html
html
  != partial("_partials/head")
  body
    != partial("_partials/header")

    main
      section.about
        .container
          h2.section-title About
          != yield
      != partial("_partials/news")
      != partial("_partials/resume")
      != partial("_partials/teaching")

  != partial("_partials/footer")
  != partial("_partials/scripts")
    
